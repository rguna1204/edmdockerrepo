Steps:

1) Push the container changes to Dcoker hub
2) Create a Github account.
3) Create a repository to git hub.
4) Create a master branch under the repository
5) Map the account with Docker.
6) Map the git hub repository and branch details in the configure Build tab
7) Build the latest image and move to Git hub repository with the master branch